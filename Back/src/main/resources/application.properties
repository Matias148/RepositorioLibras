#spring.profiles.active=devel
application.name=developer

#--------------------------------------------------
#--- DB
#--------------------------------------------------
#spring.datasource.url=jdbc:h2:mem:test
spring.datasource.url=jdbc:h2:D:\\Usuarios\\Matheus\\Desktop\\libras\\banco
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

spring.datasource.platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

##O que o Hibernate ira fazer quando a estrutura for diferente:
##update: altera a estrutura do banco
##create-drop: dropar e depois criar novamente (perdendo valores)
##validate: valida o DB, não faz alterações no banco de dados
spring.jpa.hibernate.ddl-auto=create-drop
#
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=D:\\Usuarios\\Matheus\\Desktop\\pasta